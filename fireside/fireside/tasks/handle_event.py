# __all = ["handle_event"]

# import logging

# from fireside.utils import JSONObject

# logger = logging.getLogger(__name__)


# @task(name="HandleEvent", description="Passes event to all `EventHandlers` which accept it.")
# def handle_event(*) -> JSONObject:
#     pass
